{
  "name": "site-americanfullfightinbons",
  "version": "1.0.0",
  "private": true,
  "description": "Site public AFFBC compatible Cloudflare Workers + D1.",
  "scripts": {
    "cf-typegen": "wrangler types",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm test && wrangler deploy --dry-run",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "seedLocalD1": "wrangler d1 migrations apply DB --local",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "5.9.3",
    "vitest": "^2.1.4",
    "wrangler": "4.97.0"
  }
}
